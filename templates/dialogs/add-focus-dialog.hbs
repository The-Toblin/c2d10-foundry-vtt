<form>
    <input id="focus-name" type="text" value="{{name}}" placeholder="{{localize "c2d10.skills.name"}}" />
    <select id="focus-skill">
        {{selectOptions config.allSkills selected=parent localize=true}}
    </select>
</form>

