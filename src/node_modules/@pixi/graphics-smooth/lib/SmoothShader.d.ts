import { Shader } from '@pixi/core';
import { IGraphicsBatchSettings } from './core/BatchDrawCall';
/**
 * @memberof PIXI.smooth
 */
export declare class SmoothGraphicsShader extends Shader {
    settings: IGraphicsBatchSettings;
    constructor(settings: IGraphicsBatchSettings, vert?: string, frag?: string, uniforms?: {});
    static generateVertexSrc(settings: IGraphicsBatchSettings, vertexSrc?: string): string;
    static generateFragmentSrc(settings: IGraphicsBatchSettings, fragmentSrc?: string): string;
    static generateSampleSrc(maxTextures: number): string;
}
